#include <iostream>

int main(int argc, char** argv) {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    std::cout << 2000 << "\n";
    for (int i = 0; i < 1000; i++) { std::cout << 1 << " "; }
    for (int i = 0; i < 1000; i++) { std::cout << 1000 << " "; }
return 0; }
