/*그래픽 계산기의 도움을 받음.*/
#include <iostream>
using namespace std;

int main(int argc, char** argv) {
    ios::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);
    long long a; cin >> a;
    cout << 3 * a + (a - 1) << ".6666666667";
return 0; }
