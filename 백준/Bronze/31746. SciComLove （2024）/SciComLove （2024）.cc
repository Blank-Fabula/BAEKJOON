#include <bits/stdc++.h>

int main (int argc, char *argv[])
{
    std::ios_base::sync_with_stdio(0);
    std::cin.tie(0);
    
    int N;
    std::cin >> N;

    std::cout << (N & 1 ? "evoLmoCicS" : "SciComLove") << '\n';
    
    return 0;
}