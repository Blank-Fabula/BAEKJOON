#include <iostream>
using namespace std;

int main(int argc, char** argv) {
	ios::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);
	long long N; cin >> N;
	cout << N * (N - 1) * (N - 2) / 6 << '\n';
    cout << 3;
return 0; }
